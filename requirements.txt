# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.4.0
seaborn>=0.11.0

# Audio processing
librosa>=0.9.0
soundfile>=0.10.0
pyaudio>=0.2.11

# Machine learning
scikit-learn>=1.0.0
joblib>=1.0.0

# Signal processing and wavelets
scipy>=1.7.0
PyWavelets>=1.1.1

# Progress bars
tqdm>=4.62.0

# Additional dependencies
sounddevice>=0.4.4
python-dotenv>=0.19.0
noisereduce

demucs>=4.0.0
# Demucs dependencies
# If using GPU, ensure correct torch version for your CUDA
# For CPU-only, the default torch is sufficient
# You may pin torch/torchaudio versions as needed for compatibility
torch>=1.9.0
# torchaudio is required for Demucs
# If you encounter issues, check Demucs docs for version compatibility
# soundfile is already included above

torchaudio>=0.9.0